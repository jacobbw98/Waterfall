# Agentic Program Dependencies
# Core
ollama>=0.4.0
gradio>=4.0.0
playwright>=1.40.0
python-docx>=1.1.0
Pillow>=10.0.0

# Cross-platform input control
pyautogui>=0.9.54

# Windows-only (auto-skipped on Linux)
pywin32>=306; sys_platform == 'win32'
mpmath>=1.3.0

# Short-term memory (RLM) — also used by Poetiq ARC solver
litellm>=1.0.0
RestrictedPython>=6.0
python-dotenv>=1.0.0

# Long-term memory (MemOS client)
requests>=2.28.0

# Reasoning (Poetiq ARC-AGI solver)
asynciolimiter>=1.2.0
numpy>=2.0.0
scipy>=1.10.0
